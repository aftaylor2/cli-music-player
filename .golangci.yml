linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused

linters-settings:
  govet:
    enable-all: true
    disable:
      - fieldalignment

run:
  timeout: 2m
