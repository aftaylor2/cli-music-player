package tui

// Search functionality is integrated directly into model.go via the
// textinput bubble component. This file exists as a placeholder per the
// plan structure. The search input is initialized in NewModel and
// handled in handleSearchKey.
